[project]
name = "religiao-politica-brasil"
version = "0.1.0"
description = "Estudo sobre o crescimento de políticos ligados a igrejas e religiões no Brasil."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
